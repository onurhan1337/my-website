---
type: code
createdAt: 2025-01-21T20:30:00
---

```bash
# Quick way to find large files in a project
find . -type f -size +10M -exec ls -lh {} \; | sort -k5 -hr
```

This saved me hours today when debugging a bloated Docker image. Sometimes the old Unix tools are still the best.
