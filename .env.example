# Application URL
# Used for generating absolute URLs (OG images, canonical URLs, etc.)
NEXT_PUBLIC_APP_URL=https://onurhan.dev

# Upstash Claps (Optional)
# Required for the claps feature to work
# Get these from https://console.upstash.com/
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
